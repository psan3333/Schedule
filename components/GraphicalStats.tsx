import React from "react";
import { Text, View } from "react-native";

const GraphicalStats = () => {
    return (
        <View>
            <Text>GraphicalStats</Text>
        </View>
    );
};

export default GraphicalStats;
